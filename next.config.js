/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['lh3.googleusercontent.com', 'storage.googleapis.com'], // Add your image domain(s) here
      },
}

module.exports = nextConfig
